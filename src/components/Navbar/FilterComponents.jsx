import React from 'react';

export default function FilterComponents(props) {
    return (
        <div>this filters the components to be shown</div>
    )
}

// These filters will allow the user to display only a portion
// of the component tree