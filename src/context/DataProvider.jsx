import React from 'react';

export default function DataProvider(props) {
    return (
        <div>data provider</div>
    )
}

export function useDataProvider() {
    return (
        <div>data provider hook</div>
    )
}