import React from 'react';

export default function ThemeProvider(props) {
    return (
        <div>theme provider</div>
    )
}

export function useThemeProvider() {
    return (
        <div>use theme provider</div>
    )
}